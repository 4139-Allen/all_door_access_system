切换 W5500 网口模式操作步骤

  整个架构变化

  当前 WiFi 模式:  STM32 ←UART→ ESP32-S3 ←WiFi→ MQTT Broker ←→ 后端
  W5500 网口模式:  STM32 ←SPI→ W5500 ←网线→ MQTT Broker ←→ 后端
                                            (去掉 ESP32-S3，不依赖 WiFi)

  步骤 1：硬件接线

  W5500 的引脚定义在 stm32/Hardware/W5500/w5500.h:7-20，你需要把 W5500 模块接到 STM32 的这些引脚：

  ┌────────────┬────────────┬──────────────┐
  │ W5500 引脚 │ STM32 引脚 │     说明     │
  ├────────────┼────────────┼──────────────┤
  │ SCK        │ PA1        │ SPI 时钟     │
  ├────────────┼────────────┼──────────────┤
  │ MISO       │ PA8        │ SPI 主机输入 │
  ├────────────┼────────────┼──────────────┤
  │ MOSI       │ PA11       │ SPI 主机输出 │
  ├────────────┼────────────┼──────────────┤
  │ CS         │ PA12       │ 片选         │
  ├────────────┼────────────┼──────────────┤
  │ RST        │ PC13       │ 复位         │
  ├────────────┼────────────┼──────────────┤
  │ VCC        │ 3.3V       │ 供电         │
  ├────────────┼────────────┼──────────────┤
  │ GND        │ GND        │ 共地         │
  └────────────┴────────────┴──────────────┘

  接线后 拔掉 ESP32-S3 的 UART 线（PA9/PA10），保留 USART1 的串口调试线（PA9-TX 可接 USB 转串口看 printf 输出）。

  步骤 2：改编译宏

  main.c:24：
  // 改这二行：
  1. main.c:24 — COMM_MODE_ESP8266 → COMM_MODE_W5500
  2. menu.c:17 — COMM_MODE_ESP8266 → COMM_MODE_W5500

  步骤 3：改网络配置

  main.c:42-50，改成你局域网的实际 IP：

  static w5500_netinfo_t netcfg = {
      .mac     = {0x02, 0x00, 0x00, 0x00, 0x00, 0x01}, // 确保唯一
      .ip      = {192, 168, 1, 200},    // 设备IP，根据你路由器网段改
      .gateway = {192, 168, 1, 1},      // 路由器网关
      .subnet  = {255, 255, 255, 0},    // 子网掩码
  };

  static uint8_t mqtt_broker_ip[4] = {192, 168, 1, 100};  // 后端服务器IP
  static uint16_t mqtt_broker_port = 1883;


  步骤 5：编译烧录

  把 COMM_MODE 改完后，重新编译 STM32 工程（Keil），烧录到设备。

  验证清单

  ┌───────────────────────┬─────────────────────────────────────────┐
  │        验证项         │                预期结果                 │
  ├───────────────────────┼─────────────────────────────────────────┤
  │ 插网线后 W5500 指示灯 │ 亮/闪                                   │
  ├───────────────────────┼─────────────────────────────────────────┤
  │ 上电 LCD              │ 显示 "W5500 Init..." → "MQTT Connected" │
  ├───────────────────────┼─────────────────────────────────────────┤
  │ 串口调试输出          │ 能看到 W5500 版本寄存器和连接过程       │
  ├───────────────────────┼─────────────────────────────────────────┤
  │ Web 前端远程开门      │ 继电器吸合，门禁日志有记录              │
  ├───────────────────────┼─────────────────────────────────────────┤
  │ 本地密码开门          │ 日志显示"密码开门"                      │
  ├───────────────────────┼─────────────────────────────────────────┤
  │ 本地刷卡开门          │ 日志显示"刷卡开门"                      │
  ├───────────────────────┼─────────────────────────────────────────┤
  │ 本地指纹开门          │ 日志显示"指纹开门"                      │
  └───────────────────────┴─────────────────────────────────────────┘

  如果连接不上，排查顺序

  1. ping 设备IP — 确认 W5500 初始化成功（看串口输出 VERSION register = 0x04）
  2. ping 网关 — ARP 通说明网线/交换机正常
  3. telnet MQTT端口 — telnet 后端IP 1883 确认 MQTT Broker 可达
  4. 检查 mqtt_broker_ip 是否填对了后端 IP（跑 ipconfig 看）